package model;

public class Choreography {

    // Fields: skater name
    //         falls
    //         deductions
    //         duration
    //         type
    //         skating skills component

    // Methods: setSkaterName();
    //          getSkaterName();
    //          addDeduction(int i); -if no i, add 1 fall automatically
    //          getDeduction();
    //          addFall(int i); -if no i, add 1 fall automatically
    //          getFall();
    //          setDuration(int i);
    //          getDuration();
    //          setType(boolean b);
    //          getType();
    //          contains(Element e);
    //          addElement(Element e, int i); -if no i, add the element at the end automatically
    //          removeElement(int i); -if no i, remove the element at the end automatically
    //          size();

}
